export const sum = (arr: Array<number>, defaultValue: number) => arr.reduce((acc, cur) => acc + cur, defaultValue)

export {default as Button} from './src/Button'
export {default as Title} from './src/Title'
